import { ExtensionContext } from "vscode";

import { getWakeExecCommand, getWorkspaceConfig } from './utils';
import { outputChannel } from './status';

export async function activateWls(context: ExtensionContext) {
    // TODO
}

export async function deactivateWls() {
    // TODO
}